fta() {
  local socket socket_dir

  socket_dir="/tmp/tmux-$(id -u)"
  socket=$(fd . "$socket_dir" | xargs -I{} basename {} | fzf)

  [[ -z "$socket" ]] && return 1

  tmux -S "$socket_dir/$socket" attach
}
