unbak() {
  local bak=$1
  case "$bak" in
    *.BAK)
      mv $bak `echo $bak | sed -e 's/.BAK//'`
      ;;
    *.bak)
      mv $bak `echo $bak | sed -e 's/.bak//'`
      ;;
    *)
      return 1
      ;;
  esac
}
